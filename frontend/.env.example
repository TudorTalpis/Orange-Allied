# Base URL of the future FastAPI backend.
VITE_API_URL=http://localhost:8000/api

# When "true" the frontend runs entirely against the in-memory mock services.
# Set to "false" once the backend above is available.
VITE_USE_MOCK_API=true
